import React from 'react';
import ReactDOM from 'react-dom/client';
import Atest from './A-test/test-a';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Atest />
  </React.StrictMode>
);
